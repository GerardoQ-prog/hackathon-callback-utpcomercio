"use client"

import styled from "styled-components";
import { ContainerFilters } from "./styled";

const MapFilters = () => {
  return (
    <ContainerFilters>
      aasdas
    </ContainerFilters>
  )
}

export default MapFilters